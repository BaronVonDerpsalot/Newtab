# Optional build-time configuration. Copy to `.env` to override defaults.
# All values fall back to the originals baked into src/config.js if unset.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_USER_ID=
VITE_PASSWORD=
VITE_APP_TOKEN=
